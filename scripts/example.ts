/**
 * Example script to demonstrate the structure
 */

(async () => {
  console.log("This is an example script!");

  await new Promise(() => {
    console.log("write promises in here, as desired");
  });
})();
